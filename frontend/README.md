# Frontend (Smart Trip Planner)

This folder contains a minimal React + Vite + Tailwind scaffold for the Smart Trip Planner frontend.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The app expects the backend API to be available at the environment variable `VITE_API_BASE_URL` (defaults to `http://localhost:4000/api`).
